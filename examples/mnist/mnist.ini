[model]
cfg = ../cfg/lecnn.cfg
tmp_dir = ../../models/tmp
save_dir = ../../models
loss = cross_entropy

[data]
save_path = ../../../dataset

[hypers]
lr = 0.01
momentum = 0.9
batch_sz = 256
log_freq = 20
num_workers = 2
