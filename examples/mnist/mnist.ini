[model]
cfg = ../cfg/lecnn.cfg
save_dir = ../../models
loss = cross_entropy

[data]
save_path = ../../../dataset

[hypers]
epochs = 1
lr = 0.01
momentum = 0.9
weight_decay=0.0001
batch_sz = 256
log_freq = 20
num_workers = 2
